# qmisc
Common header files for C/C++ projects. Focus on speed and simplicity. Other repos here may depend on this repo existing alongside it.
# pre-commit
`pre-commit` is a shell script that is used to reject commits that fail during `make tests` use the below command to link it to your copy of the repository
> ln -s ../../pre-commit .git/hooks/pre-commit
